<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.wcpe.wcpelib.bukkit.mapper.PlayerDailyLoginMapper">

    <resultMap id="PlayerDailyLoginDataResultMap" type="top.wcpe.wcpelib.bukkit.entity.PlayerDailyLoginData">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="stat_date" property="statDate" jdbcType="DATE" javaType="java.time.LocalDate"/>
        <result column="player_uuids" property="playerUuids" jdbcType="LONGVARCHAR"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, stat_date, player_uuids
    </sql>

    <update id="createTable">
        CREATE TABLE IF NOT EXISTS `wpcelib_player_daily_login` (
          `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
          `stat_date` DATE NOT NULL COMMENT '统计日期(yyyy-MM-dd)',
          `player_uuids` LONGTEXT NOT NULL COMMENT '当日登录玩家UUID列表，逗号分隔',
          PRIMARY KEY (`id`),
          UNIQUE KEY `uk_stat_date` (`stat_date`)
        ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
    </update>

    <insert id="upsert" parameterType="top.wcpe.wcpelib.bukkit.entity.PlayerDailyLoginData">
        INSERT INTO `wpcelib_player_daily_login` (stat_date, player_uuids)
        VALUES (#{statDate,jdbcType=DATE,javaType=java.time.LocalDate},
                #{playerUuids,jdbcType=LONGVARCHAR})
        ON DUPLICATE KEY UPDATE
            player_uuids = VALUES(player_uuids);
    </insert>

    <select id="getByStatDate" resultMap="PlayerDailyLoginDataResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM `wpcelib_player_daily_login`
        WHERE stat_date = #{statDate,jdbcType=DATE,javaType=java.time.LocalDate}
        LIMIT 1
    </select>
</mapper>
